# Query pack — trend-radar

Use as starting seeds. Drop noise; add user overrides. Prefer last 7–14 days.

## AIUxer (experience / GenUI / trust)

### Reddit
- `site:reddit.com/r/UXDesign generative UI OR agentic UX OR "human in the loop"`
- `site:reddit.com/r/UXDesign "AI slop" OR chat composer OR citations`
- `site:reddit.com/r/MachineLearning "generative UI" OR "structured output" UI`
- `site:reddit.com (generative UI OR "AI-native" interface) (trust OR grounding OR confirm)`

### X
- `generative UI OR "agentic UX" OR "HITL" OR "closed vocabulary" OR "AI UX patterns"`
- `("streaming UI" OR "tool use" UI) (agent OR LLM)`
- Accounts as optional boost (not exclusive): practitioners shipping AI products

### GitHub
- Topics / search: `generative-ui`, `generative ui`, `ai-sdk` generative UI
- `agentic-ux` OR `ai-ux` OR `openui` OR `ag-ui` OR `a2ui`
- Repos: recently updated with README mentioning component registry / allowlist / structured UI
- `awesome-generative-ui` and similar lists — use as **indexes**, not as signals themselves

## AIEngineer (architecture / evals / cost / agents)

### Reddit
- `site:reddit.com/r/LocalLLaMA (evals OR "LLM judge" OR agent) (production OR cost)`
- `site:reddit.com/r/LangChain OR site:reddit.com/r/MachineLearning agent workflow OR multi-agent cost`
- `site:reddit.com ("building effective agents" OR "context engineering" OR "prompt caching")`

### X
- `("AI agents" OR "agentic") (evals OR reliability OR "cost per" OR production)`
- `("context engineering" OR "LLM observability" OR "dual gate" OR "structured output")`
- Anthropic / Hamel / eng blogs often surface as links — follow the **claim**, not the brand

### GitHub
- Topics: `llm-eval`, `llm-evaluation`, `ai-agents`, `agent-framework`, `llmops`, `promptfoo`
- `awesome-evals`, harness/observability repos with recent commits
- Search: `workflow OR router agent` LLM production
- New agent protocols / standards (AG-UI, MCP apps, A2UI) — architecture relevance

## Shared (both lenses)
- Autonomy / confirm / "agent deleted" / runaway cost postmortems
- Registry / schema validation / "model emitted unknown type"
- Memory: session vs durable, inspectable memory

## Explicit noise (usually drop)
- Funding rounds, model leaderboard flex without product method
- "Cursor/Claude replaces all developers" culture war
- Empty `create-react-app` + one LLM call scaffolds with no docs
- Duplicate of a signal already in last 3 radar days
